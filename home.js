import { menus } from "./data/data.js";
// let menus = require("./data/data");

let starter = document.querySelector("#starter");

menus.forEach(menu => {
    console.log(menu)
    // starter.innerHTML += `<div>`
})

